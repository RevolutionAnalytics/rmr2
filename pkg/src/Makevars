BINDIR	= inst/hbase-io
.PHONY: all hbase-io

PKG_CXXFLAGS=`${R_HOME}/bin/Rscript -e "Rcpp:::CxxFlags()"`
PKG_LIBS = `$(R_HOME)/bin/Rscript -e "Rcpp:::LdFlags()"`

all: $(SHLIB) hbase-io

hbase-io:
	(cd hbase-io; sh build_linux.sh)
	
clean:
	echo "not implemented yet"
